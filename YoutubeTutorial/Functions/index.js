const { getDish, getDishes } = require("./Dishes");
const { getComment, getComments, addComment } = require("./Comments");

module.exports = {
  getDish,
  getDishes,
  getComment,
  getComments,
  addComment
}